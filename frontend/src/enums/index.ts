export enum RESULT_TYPE {
  SO1SCAN = "so1scan",
  SLITHER = "slither",
  MYTHRIL = "mythril",
}

export enum SEVERITY {
  INFORMATIONAL = "informational",
  LOW = "low",
  MEDIUM = "medium",
  HIGH = "high",
  CRITICAL = "critical",
}

export enum TOOL {
  So1Scan,
  Slither,
  Mythril,
}
